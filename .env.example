# ============================================================================
# IA-Core-Tools Configuration Example
# ============================================================================
# Copy this file to .env and fill in your actual values

# ----------------------------------------------------------------------------
# Database Configuration
# ----------------------------------------------------------------------------
SQLALCHEMY_DATABASE_URI=postgresql://${DATABASE_USER}:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=iacore
DATABASE_PASSWORD=iacore
DATABASE_NAME=iacore

# ----------------------------------------------------------------------------
# Application URLs
# ----------------------------------------------------------------------------
BACKEND_URL=http://localhost:8000
FRONTEND_URL=http://localhost:5173

# ----------------------------------------------------------------------------
# Authentication Configuration
# ----------------------------------------------------------------------------

# AICT_LOGIN: Controls the authentication mode
# Options:
#   - OIDC: Production mode using EntraID/Azure AD authentication (default)
#   - FAKE: Development/testing mode with simple email-based login
# Default: OIDC
AICT_LOGIN=OIDC

# EntraID/Azure AD Configuration (REQUIRED when AICT_LOGIN=OIDC)
# When using OIDC mode, you MUST provide these credentials:
ENTRA_TENANT_ID=your-tenant-id-here
ENTRA_CLIENT_ID=your-client-id-here
ENTRA_CLIENT_SECRET=your-client-secret-here

# Optional EntraID Configuration
# ENTRA_TOKEN_TYPE=ID_TOKEN
# ENTRA_VALIDATE_AUDIENCE=true
# ENTRA_VALIDATE_ISSUER=true
# ENTRA_LEEWAY=0
# ENTRA_API_VERSION=v1.0

# When AICT_LOGIN=FAKE (Development Mode):
# - EntraID credentials are NOT required
# - Users can login with just their email (must exist in database)
# - ⚠️ WARNING: Only use FAKE mode for development/testing, NEVER in production!
#
# Example for FAKE mode:
# AICT_LOGIN=FAKE
# # EntraID credentials not needed - comment them out or remove them

# ----------------------------------------------------------------------------
# Legacy OAuth Configuration (Optional - for backward compatibility)
# ----------------------------------------------------------------------------
# Google OAuth (if you still need it)
# GOOGLE_CLIENT_ID=your_google_client_id_here
# GOOGLE_CLIENT_SECRET=your_google_client_secret_here
# GOOGLE_DISCOVERY_URL=https://accounts.google.com/.well-known/openid-configuration
# GOOGLE_REDIRECT_URI=${BACKEND_URL}/auth/callback

# ----------------------------------------------------------------------------
# Security Configuration
# ----------------------------------------------------------------------------
SECRET_KEY=your-secret-key-change-this-in-production

# JWT Configuration
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# ----------------------------------------------------------------------------
# Admin Configuration
# ----------------------------------------------------------------------------
# Comma-separated list of admin emails
AICT_OMNIADMINS=admin@example.com

# ----------------------------------------------------------------------------
# Application Configuration
# ----------------------------------------------------------------------------
APP_PORT=8000
AICT_MODE=SELF-HOSTED  # Options: SELF-HOSTED, SAAS

# ----------------------------------------------------------------------------
# File Storage
# ----------------------------------------------------------------------------
REPO_BASE_FOLDER=data/repositories
TMP_BASE_FOLDER=data/tmp

# ----------------------------------------------------------------------------
# AI Service API Keys (Optional)
# ----------------------------------------------------------------------------
# Uncomment and fill in the API keys for the services you want to use
# OPENAI_API_KEY=your_openai_api_key_here
# ANTHROPIC_API_KEY=your_anthropic_api_key_here
# AZURE_OPENAI_API_KEY=your_azure_openai_api_key_here
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
# MISTRAL_API_KEY=your_mistral_api_key_here

# ----------------------------------------------------------------------------
# Vector Database Configuration (Optional)
# ----------------------------------------------------------------------------
# Supported types: PGVECTOR, QDRANT, PINECONE, WEAVIATE, CHROMA
# VECTOR_DB_TYPE=PGVECTOR

# Qdrant Configuration (if using QDRANT)
# QDRANT_URL=http://localhost:6333
# QDRANT_API_KEY=your-qdrant-api-key
# QDRANT_PREFER_GRPC=false

# Pinecone Configuration (if using PINECONE)
# PINECONE_API_KEY=your-pinecone-api-key
# PINECONE_ENVIRONMENT=your-pinecone-environment

# Weaviate Configuration (if using WEAVIATE)
# WEAVIATE_URL=http://localhost:8080
# WEAVIATE_API_KEY=your-weaviate-api-key

# Chroma Configuration (if using CHROMA)
# CHROMA_PERSIST_DIR=./chroma_db

# ----------------------------------------------------------------------------
# CORS Configuration (Optional)
# ----------------------------------------------------------------------------
CORS_ORIGIN_DEV_SERVER=${FRONTEND_URL}
CORS_ORIGIN_DEV_SERVER_ALT=http://127.0.0.1:5173
CORS_ORIGIN_DOCKER=http://localhost:3000
CORS_ORIGIN_DOCKER_ALT=http://127.0.0.1:3000
# Additional dev ports (enabled automatically in non-OIDC modes)
# CORS_ORIGIN_DEV_8080=http://localhost:8080
# CORS_ORIGIN_DEV_8080_ALT=http://127.0.0.1:8080
# CORS_ORIGIN_VITE_PREVIEW=http://localhost:4173
# CORS_ORIGIN_VITE_PREVIEW_ALT=http://127.0.0.1:4173

# ============================================================================
# DEPRECATED VARIABLES (No longer used)
# ============================================================================
# The following variables have been removed and are no longer used:
# - OIDC_ENABLED: Replaced by AICT_LOGIN (use AICT_LOGIN=OIDC or AICT_LOGIN=FAKE)
# - DEVELOPMENT_MODE: No longer used in the codebase
#
# If you have these in your .env file, please remove them and use AICT_LOGIN instead.
# ============================================================================

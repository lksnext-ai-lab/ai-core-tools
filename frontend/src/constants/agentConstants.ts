// Agent configuration constants
export const DEFAULT_AGENT_TEMPERATURE = 0.7;

export const DEFAULT_MEMORY_SUMMARIZE_THRESHOLD = 20;

// Must contain {question}: the backend formats the user message into it.
export const DEFAULT_PROMPT_TEMPLATE = '{question}';

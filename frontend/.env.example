# API Configuration
VITE_API_BASE_URL=http://localhost:8000

# OIDC Authentication (EntraID/Azure AD)
# Enable OIDC authentication
# Set to 'false' for development mode (simple email-based login)
# Set to 'true' for production (Azure AD OIDC authentication)
VITE_OIDC_ENABLED=true

# Azure AD Authority URL - replace {tenant-id} with your Azure AD tenant ID
# For multi-tenant: https://login.microsoftonline.com/common/v2.0
# For specific tenant: https://login.microsoftonline.com/{tenant-id}/v2.0
VITE_OIDC_AUTHORITY=https://login.microsoftonline.com/{tenant-id}/v2.0

# Azure AD Application (client) ID from Azure Portal
VITE_OIDC_CLIENT_ID=your-azure-client-id

# Redirect URI after authentication (must match Azure AD app registration)
VITE_OIDC_REDIRECT_URI=http://localhost:5173/auth/success

# OIDC Scopes - default includes OpenID Connect standard scopes
VITE_OIDC_SCOPE=openid profile email

# API Audience - the identifier of the API resource you want to access
# This is typically the Application ID URI configured in Azure AD
VITE_OIDC_AUDIENCE=api://4c151d3b-b6c9-4835-88e1-39412d31a443

# ============================================
# DEVELOPMENT MODE AUTHENTICATION
# ============================================
# For local development without Azure AD:
# 1. Set VITE_OIDC_ENABLED=false (above)
# 2. Backend: Set DEVELOPMENT_MODE=true in backend/.env
# 3. Backend: Run python -m utils.seed_dev_users to create test users
# 4. Login with any seeded email (e.g., admin@example.com)
#
# Default test users created by seeding script:
# - admin@example.com
# - user1@example.com
# - user2@example.com
# - dev@example.com
# ============================================

# --------------------------------------------
# DEVELOPMENT MODE AUTHENTICATION
# --------------------------------------------
# For local development without Azure AD:
# 1. Set VITE_OIDC_ENABLED=false
# 2. Backend: Set DEVELOPMENT_MODE=true in backend/.env
# 3. Backend: Run python -m utils.seed_dev_users to create test users
# 4. Login with any seeded email (e.g., admin@example.com)
#
# Default test users created by seeding script:
# - admin@example.com
# - user1@example.com
# - user2@example.com
# - dev@example.com
# --------------------------------------------
